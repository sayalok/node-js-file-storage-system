const uploadFile = () => {
	return true;
}

const getFileByPubKey = (publicKey) => {
	return true;
}

const deleteFileByPrivateKey = (privateKey) => {
	return true;
}

module.exports = {
	uploadFile,
	getFileByPubKey,
	deleteFileByPrivateKey
};